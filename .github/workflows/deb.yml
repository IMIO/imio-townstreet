name: Deb build push notify
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deb:
    uses: IMIO/gha-workflows/.github/workflows/deb-build-push-notify.yml@DEVOPS-417
    secrets: inherit
    with:
      distributions: '["bookworm", "trixie"]'
      package_install_path: '/usr/lib'